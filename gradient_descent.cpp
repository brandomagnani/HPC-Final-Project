#include <stdio.h>
#include <math.h>
// #include <omp.h>
#include <stdlib.h>

void residual(double* A, double* x, double* b, double* r)
{

}

void gradient(double* A, double* r, long n, double* grad)
{
    
}

void gradient_iteration(double* A, double* x, double* b, double eta)
{
    // double* b = (double*) malloc(n * sizeof(double));
}

void gradient_descent(double* A, double* x, double* b, double eta, long n_iter)
{

}

double norm(double* r)
{
    float mag;
    return mag;
}